# Bio-Audit 环境变量模板（H9：仅环境变量注入，仓库零密钥）
# 复制为 .env 并按需填写；.env 已被 .gitignore 强制忽略。

# 事件日志目录（默认 ~/.bioaudit/logs/events）
# BIOAUDIT_LOG_DIR=C:\Users\you\.bioaudit\logs\events

# ── 阶段 2+ 预留（MCP/采集密钥一律走环境变量，禁止写入代码/仓库）──
# ANTHROPIC_API_KEY=
# DEEPSEEK_API_KEY=
